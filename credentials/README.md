# Place your Google OAuth **Web application** client JSON here as:
#   client_secret.json
#
# In Google Cloud Console, add Authorized redirect URIs:
#   http://127.0.0.1:8000/api/auth/google/callback
#   https://YOUR-HOST/api/auth/google/callback
#
# After Connect Google, token.json will also appear in this folder.
# Both files are gitignored.
